project('libaction', 'cpp',
        default_options : ['buildtype=release', 'cpp_std=c++11'])

deptensorflow_lite = dependency('tensorflow-lite')
libaction_include = include_directories('include')

subdir('src')
subdir('demo')
